name: task0

on:
  push:
    branches: [master]

  schedule:
    - cron: "0 06 * * *"

jobs:
  clone:

    runs-on: ubuntu-latest

    steps:
      - name: "Checkout"
        uses: actions/checkout@v3
      - name: "directory changes"
        run: cd module3_task0 && make help